import { es } from "../../../../../lib/i18n/es";

/** Balances tab — placeholder. Real content lands in T066. */
export default function BalancesTabPage() {
  return <p className="text-sm text-muted-foreground">{es.common.comingSoon}</p>;
}
